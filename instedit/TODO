- don't allow play if changes are to be made
- fix error handling for WavSet::save() and others
- store flac data

UI:
- fix file dialog mask (allow .wav, .flac, ...)
- plot tuning line
- combobox should support scroll wheel
- thread::join() sometimes blocks InstEditWindow close
- menu + play drawing looks bad
- user index should shallow load
- implement revert
- eliminate load index
- split insteditwindow in hdr + code

SVEN:
- display total sample time

PROJECT:
- cache:
  - better primary key
  - cleanup old stuff automatically
- load LV2 via integer
- somewhat better error handling
- ensure never to play old insts
