cleanup:
 - don't hardcode colors
 - make stuff private
 - replace callbacks with signals
 - redraw only if necessary

widgets:
 - popup menu
 - knob
 - led

general:
 - message box for error messages *
 - preserve scroll position on fold/...
 - recover after loading junk
 - add output if necessary (morph_plan->ensure_output())
 - scroll wheel propagation
 - combobox doesn't work at bottom of screen
 - post resize and signal_update possibly occur in wrong order

beast:
 - init with default plan

windows:
 - 32bit compile crashes in apply window sse code - why?
 - zoom doesn't resize windows in cubase
 - keyboard input broken
 - dpi scaling
 - led sometimes wrong
