Install devel tools:

$ xcode-select --install

You also need these brew packages

$ brew install autoconf-archive automake boost ccache cmake jack libao pkg-config libtool 

Finally: edit macos/sdk-options.sh

$ cd macos
$ ./build-deps.sh
$ ./build.sh
