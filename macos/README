Install devel tools:

$ xcode-select --install

You also need these brew packages

$ brew leaves

autoconf-archive
automake
boost
ccache
cmake
fftw
jack
libao
libtool
pkg-config
#lv2
#qt
#cairo

Finally: edit macos/sdk-options.sh

$ cd macos
$ ./build-deps.sh
$ ./build.sh
